# Snowflake hands-on 5

Cleanup uses the cumulative configuration from Hands-on 4. Run `terraform plan -destroy` and `terraform destroy` from the Hands-on 4 root module, then verify that only the participant-scoped resources are removed.
