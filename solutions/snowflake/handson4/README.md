# Snowflake Hands-on 4

Refines the cumulative Snowflake configuration with typed variables, validation, outputs, and explicit dependency examples.

## Usage

Provide `user_suffix` through `terraform.tfvars`. Optional variables include `warehouse_size`, `warehouse_auto_suspend`, and `table_comment`. Run `terraform validate`, `terraform plan`, and `terraform output` to inspect the resulting configuration.
